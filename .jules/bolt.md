## 2026-03-24 - [Vectorized Context Addition] **Learning:** Iteratively calling context enrichment (with date parsing and mutate) on hundreds of small data frames before combining them creates a massive performance bottleneck. **Action:** Lift context addition after combining data (via `list_rbind`) and use vectorized metadata mapping. Pre-parse unique date strings to minimize `as.POSIXct` overhead.
